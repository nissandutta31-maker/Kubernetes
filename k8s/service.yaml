apiVersion: v1
kind: Service
metadata:
  name: nvidia-demo-app
  namespace: nvidia-runtime-demo
  labels:
    app: nvidia-demo-app
spec:
  type: ClusterIP
  selector:
    app: nvidia-demo-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
